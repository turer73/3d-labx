---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Header from "../../components/Header.astro";
import { getApiBase } from "../../lib/api";

export const prerender = false;

const API_BASE = getApiBase(Astro.request);

// Forum kategorilerini çek
let categories: any[] = [];
let recentThreads: any[] = [];
let featuredProjects: any[] = [];
let error: string | null = null;

try {
  const [catRes, threadsRes, projectsRes] = await Promise.all([
    fetch(`${API_BASE}/api/forum/categories`),
    fetch(`${API_BASE}/api/forum/categories/sorular/threads?limit=5`),
    fetch(`${API_BASE}/api/projects?limit=6&sort=popular`)
  ]);

  if (catRes.ok) {
    const catData = await catRes.json();
    categories = catData.categories || [];
  }

  if (threadsRes.ok) {
    const threadsData = await threadsRes.json();
    recentThreads = threadsData.threads || [];
  }

  if (projectsRes.ok) {
    const projectsData = await projectsRes.json();
    featuredProjects = projectsData.projects || [];
  }
} catch (e) {
  error = e instanceof Error ? e.message : "Bilinmeyen hata";
}
---

<BaseLayout 
  title="Topluluk - 3D-labX | Forum, Projeler, Tartışmalar"
  description="3D baskı topluluğuna katılın. Sorularınızı sorun, projelerinizi paylaşın, diğer makerlarla tanışın."
>
  <Header />

  <main class="community-page">
    <div class="container">
      <!-- Hero Section -->
      <section class="community-hero">
        <div class="hero-content">
          <div class="hero-badge">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
            <span>Topluluk</span>
          </div>
          <h1>3D Baskı Topluluğu</h1>
          <p>Sorularınızı sorun, projelerinizi paylaşın, diğer makerlarla tanışın</p>
          <div class="hero-actions">
            <a href="/topluluk/forum" class="btn btn-primary">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
              Foruma Git
            </a>
            <a href="/topluluk/projeler" class="btn btn-secondary">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>
              Projeleri Keşfet
            </a>
          </div>
        </div>
        <div class="hero-stats">
          <div class="stat-item">
            <span class="stat-icon">💬</span>
            <span class="stat-value" id="threadCount">-</span>
            <span class="stat-label">Konu</span>
          </div>
          <div class="stat-item">
            <span class="stat-icon">🎨</span>
            <span class="stat-value" id="projectCount">-</span>
            <span class="stat-label">Proje</span>
          </div>
          <div class="stat-item">
            <span class="stat-icon">👥</span>
            <span class="stat-value" id="userCount">-</span>
            <span class="stat-label">Üye</span>
          </div>
        </div>
      </section>

      <!-- Bento Grid Layout -->
      <div class="community-bento">
        <!-- Forum Kategorileri -->
        <section class="bento-section forum-categories">
          <div class="section-header">
            <h2>
              <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
              Forum
            </h2>
            <a href="/topluluk/forum" class="view-all">Tümünü Gör →</a>
          </div>
          <div class="categories-grid">
            {categories.map((cat) => (
              <a href={`/topluluk/forum/${cat.slug}`} class="category-card" style={`--cat-color: ${cat.color}`}>
                <div class="cat-icon">{cat.icon}</div>
                <div class="cat-info">
                  <h3>{cat.name}</h3>
                  <p>{cat.description}</p>
                </div>
                <div class="cat-stats">
                  <span>{cat.thread_count} konu</span>
                  <span>{cat.post_count} mesaj</span>
                </div>
              </a>
            ))}
          </div>
        </section>

        <!-- Son Konular -->
        <section class="bento-section recent-threads">
          <div class="section-header">
            <h2>
              <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
              Son Konular
            </h2>
          </div>
          <div class="threads-list">
            {recentThreads.length > 0 ? recentThreads.map((thread) => (
              <a href={`/topluluk/forum/konu/${thread.slug}`} class="thread-item">
                <div class="thread-avatar">
                  {thread.avatar_url ? (
                    <img src={thread.avatar_url} alt={thread.username} />
                  ) : (
                    <div class="avatar-placeholder">{thread.username?.charAt(0).toUpperCase()}</div>
                  )}
                </div>
                <div class="thread-info">
                  <h4>{thread.title}</h4>
                  <div class="thread-meta">
                    <span class="thread-author">@{thread.username}</span>
                    <span class="thread-replies">{thread.reply_count} cevap</span>
                    {thread.is_solved === 1 && <span class="thread-solved">✓ Çözüldü</span>}
                  </div>
                </div>
              </a>
            )) : (
              <div class="empty-state">
                <p>Henüz konu yok. İlk konuyu sen aç!</p>
                <a href="/topluluk/forum/yeni" class="btn btn-sm">Konu Aç</a>
              </div>
            )}
          </div>
        </section>

        <!-- Projeler Showcase -->
        <section class="bento-section projects-showcase">
          <div class="section-header">
            <h2>
              <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>
              Projeler
            </h2>
            <a href="/topluluk/projeler" class="view-all">Tümünü Gör →</a>
          </div>
          <div class="projects-grid">
            {featuredProjects.length > 0 ? featuredProjects.map((project) => {
              const images = JSON.parse(project.images || '[]');
              const firstImage = images[0];
              return (
                <a href={`/topluluk/projeler/${project.slug}`} class="project-card">
                  <div class="project-image">
                    {firstImage ? (
                      <img src={firstImage} alt={project.title} loading="lazy" />
                    ) : (
                      <div class="no-image">
                        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>
                      </div>
                    )}
                    {project.is_featured === 1 && <span class="featured-badge">⭐ Öne Çıkan</span>}
                  </div>
                  <div class="project-info">
                    <h4>{project.title}</h4>
                    <div class="project-meta">
                      <span>@{project.username}</span>
                      <span>❤️ {project.like_count}</span>
                    </div>
                  </div>
                </a>
              );
            }) : (
              <div class="empty-state full-width">
                <p>Henüz proje paylaşılmamış. İlk projeyi sen paylaş!</p>
                <a href="/topluluk/projeler/yeni" class="btn btn-sm">Proje Paylaş</a>
              </div>
            )}
          </div>
        </section>

        <!-- Hızlı Aksiyonlar -->
        <section class="bento-section quick-actions">
          <div class="action-card ask-question">
            <div class="action-icon">❓</div>
            <h3>Soru Sor</h3>
            <p>3D baskı ile ilgili sorularınızı sorun</p>
            <a href="/topluluk/forum/yeni?category=sorular" class="btn btn-action">Soru Sor</a>
          </div>
          <div class="action-card share-project">
            <div class="action-icon">🎨</div>
            <h3>Proje Paylaş</h3>
            <p>Baskı projelerinizi toplulukla paylaşın</p>
            <a href="/topluluk/projeler/yeni" class="btn btn-action">Proje Paylaş</a>
          </div>
          <div class="action-card join-discussion">
            <div class="action-icon">💬</div>
            <h3>Tartışmalara Katıl</h3>
            <p>Güncel konularda fikirlerinizi paylaşın</p>
            <a href="/topluluk/forum/tartismalar" class="btn btn-action">Tartışmalar</a>
          </div>
        </section>
      </div>
    </div>
  </main>
</BaseLayout>

<style>
  .community-page {
    padding-top: 140px;
    padding-bottom: 64px;
    min-height: 100vh;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  /* Hero Section */
  .community-hero {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 40px;
    align-items: center;
    padding: 40px;
    background: linear-gradient(135deg, rgba(249, 115, 22, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
    border: 1px solid var(--border-soft);
    border-radius: 24px;
    margin-bottom: 32px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 14px;
    background: linear-gradient(135deg, rgba(249, 115, 22, 0.2), rgba(234, 88, 12, 0.15));
    border: 1px solid rgba(249, 115, 22, 0.3);
    border-radius: 20px;
    color: #F97316;
    font-size: 0.8rem;
    font-weight: 600;
    margin-bottom: 16px;
  }

  .hero-content h1 {
    font-size: 2.2rem;
    margin: 0 0 12px;
    background: linear-gradient(135deg, var(--text-main) 0%, var(--text-muted) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-content p {
    color: var(--text-muted);
    font-size: 1.1rem;
    margin: 0 0 24px;
  }

  .hero-actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 24px;
    border-radius: 12px;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-primary {
    background: linear-gradient(135deg, #F97316 0%, #EA580C 100%);
    color: white;
    box-shadow: 0 4px 15px rgba(249, 115, 22, 0.3);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 25px rgba(249, 115, 22, 0.4);
  }

  .btn-secondary {
    background: var(--bg-card);
    color: var(--text-main);
    border: 1px solid var(--border-soft);
  }

  .btn-secondary:hover {
    border-color: #F97316;
    color: #F97316;
  }

  .hero-stats {
    display: flex;
    gap: 24px;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 16px 24px;
    background: var(--bg-card);
    border: 1px solid var(--border-soft);
    border-radius: 16px;
    min-width: 100px;
  }

  .stat-icon {
    font-size: 1.5rem;
    margin-bottom: 4px;
  }

  .stat-value {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--text-main);
  }

  .stat-label {
    font-size: 0.8rem;
    color: var(--text-muted);
  }

  /* Bento Grid */
  .community-bento {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }

  .bento-section {
    background: var(--bg-card);
    border: 1px solid var(--border-soft);
    border-radius: 20px;
    padding: 24px;
  }

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }

  .section-header h2 {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.2rem;
    margin: 0;
    color: var(--text-main);
  }

  .view-all {
    color: #F97316;
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
  }

  .view-all:hover {
    text-decoration: underline;
  }

  /* Forum Kategorileri */
  .forum-categories {
    grid-column: span 2;
  }

  .categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 16px;
  }

  .category-card {
    display: flex;
    flex-direction: column;
    padding: 20px;
    background: var(--bg-main);
    border: 1px solid var(--border-soft);
    border-radius: 16px;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .category-card:hover {
    border-color: var(--cat-color, #F97316);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  }

  .cat-icon {
    font-size: 2rem;
    margin-bottom: 12px;
  }

  .cat-info h3 {
    margin: 0 0 4px;
    font-size: 1rem;
    color: var(--text-main);
  }

  .cat-info p {
    margin: 0;
    font-size: 0.8rem;
    color: var(--text-muted);
    line-height: 1.4;
  }

  .cat-stats {
    display: flex;
    gap: 12px;
    margin-top: 12px;
    padding-top: 12px;
    border-top: 1px solid var(--border-soft);
    font-size: 0.75rem;
    color: var(--text-light);
  }

  /* Son Konular */
  .threads-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .thread-item {
    display: flex;
    gap: 12px;
    padding: 12px;
    background: var(--bg-main);
    border: 1px solid var(--border-soft);
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .thread-item:hover {
    border-color: #F97316;
    background: rgba(249, 115, 22, 0.05);
  }

  .thread-avatar {
    flex-shrink: 0;
  }

  .thread-avatar img,
  .avatar-placeholder {
    width: 40px;
    height: 40px;
    border-radius: 10px;
    object-fit: cover;
  }

  .avatar-placeholder {
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #F97316 0%, #EA580C 100%);
    color: white;
    font-weight: 600;
    font-size: 1rem;
  }

  .thread-info {
    flex: 1;
    min-width: 0;
  }

  .thread-info h4 {
    margin: 0 0 4px;
    font-size: 0.9rem;
    color: var(--text-main);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .thread-meta {
    display: flex;
    gap: 10px;
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  .thread-solved {
    color: #22C55E;
    font-weight: 500;
  }

  /* Projeler */
  .projects-showcase {
    grid-column: span 2;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .project-card {
    background: var(--bg-main);
    border: 1px solid var(--border-soft);
    border-radius: 16px;
    overflow: hidden;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .project-card:hover {
    border-color: #8B5CF6;
    transform: translateY(-4px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
  }

  .project-image {
    aspect-ratio: 4/3;
    position: relative;
    overflow: hidden;
  }

  .project-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
  }

  .project-card:hover .project-image img {
    transform: scale(1.05);
  }

  .no-image {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg-card);
    color: var(--text-light);
  }

  .featured-badge {
    position: absolute;
    top: 10px;
    left: 10px;
    padding: 4px 10px;
    background: rgba(0, 0, 0, 0.7);
    color: #EAB308;
    font-size: 0.7rem;
    font-weight: 600;
    border-radius: 8px;
  }

  .project-info {
    padding: 14px;
  }

  .project-info h4 {
    margin: 0 0 6px;
    font-size: 0.9rem;
    color: var(--text-main);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .project-meta {
    display: flex;
    justify-content: space-between;
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  /* Quick Actions */
  .quick-actions {
    grid-column: span 2;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    padding: 20px;
  }

  .action-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 24px;
    background: var(--bg-main);
    border: 1px solid var(--border-soft);
    border-radius: 16px;
    transition: all 0.3s ease;
  }

  .action-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .action-icon {
    font-size: 2.5rem;
    margin-bottom: 12px;
  }

  .action-card h3 {
    margin: 0 0 8px;
    font-size: 1.1rem;
    color: var(--text-main);
  }

  .action-card p {
    margin: 0 0 16px;
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  .btn-action {
    padding: 10px 20px;
    background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
    color: white;
    border: none;
    border-radius: 10px;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .ask-question .btn-action {
    background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
  }

  .share-project .btn-action {
    background: linear-gradient(135deg, #8B5CF6 0%, #7C3AED 100%);
  }

  .join-discussion .btn-action {
    background: linear-gradient(135deg, #10B981 0%, #059669 100%);
  }

  .btn-action:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  }

  /* Empty State */
  .empty-state {
    text-align: center;
    padding: 24px;
    color: var(--text-muted);
  }

  .empty-state.full-width {
    grid-column: span 3;
  }

  .btn-sm {
    padding: 8px 16px;
    font-size: 0.85rem;
    background: var(--bg-card);
    color: var(--text-main);
    border: 1px solid var(--border-soft);
    border-radius: 8px;
    text-decoration: none;
    margin-top: 12px;
    display: inline-block;
  }

  .btn-sm:hover {
    border-color: #F97316;
    color: #F97316;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .community-hero {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-stats {
      justify-content: center;
    }

    .community-bento {
      grid-template-columns: 1fr;
    }

    .forum-categories,
    .projects-showcase,
    .quick-actions {
      grid-column: span 1;
    }

    .projects-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .quick-actions {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .community-page {
      padding-top: 120px;
    }

    .community-hero {
      padding: 24px;
    }

    .hero-content h1 {
      font-size: 1.6rem;
    }

    .hero-stats {
      flex-direction: row;
      flex-wrap: wrap;
    }

    .stat-item {
      min-width: 80px;
      padding: 12px 16px;
    }

    .categories-grid {
      grid-template-columns: 1fr;
    }

    .projects-grid {
      grid-template-columns: 1fr;
    }

    .empty-state.full-width {
      grid-column: span 1;
    }
  }
</style>

<script>
  const API_URL = 'https://tech-portal-api.turgut-d01.workers.dev';

  // İstatistikleri yükle
  async function loadStats() {
    try {
      // Basit bir stats endpoint'i olmadığı için kategorilerden hesapla
      const catRes = await fetch(`${API_URL}/api/forum/categories`);
      if (catRes.ok) {
        const data = await catRes.json();
        const categories = data.categories || [];
        
        const totalThreads = categories.reduce((sum: number, c: any) => sum + (c.thread_count || 0), 0);
        const totalPosts = categories.reduce((sum: number, c: any) => sum + (c.post_count || 0), 0);
        
        const threadEl = document.getElementById('threadCount');
        if (threadEl) threadEl.textContent = totalThreads.toString();
      }

      const projectRes = await fetch(`${API_URL}/api/projects?limit=1`);
      if (projectRes.ok) {
        const data = await projectRes.json();
        const projectEl = document.getElementById('projectCount');
        if (projectEl) projectEl.textContent = (data.pagination?.total || 0).toString();
      }

      // User count için basit bir değer
      const userEl = document.getElementById('userCount');
      if (userEl) userEl.textContent = '50+';
    } catch (err) {
      console.error('Stats load error:', err);
    }
  }

  loadStats();
</script>
