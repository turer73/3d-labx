---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Header from "../../components/Header.astro";
import { getLanguage, type Language } from "../../lib/api";

export const prerender = false;

const lang = getLanguage(Astro.request);

const translations: Record<Language, {
  pageTitle: string;
  pageDesc: string;
  title: string;
  subtitle: string;
  usernameLabel: string;
  usernamePlaceholder: string;
  usernameHint: string;
  emailLabel: string;
  emailPlaceholder: string;
  passwordLabel: string;
  passwordPlaceholder: string;
  passwordConfirmLabel: string;
  passwordConfirmPlaceholder: string;
  showPassword: string;
  privacyPolicy: string;
  privacyAccept: string;
  registerButton: string;
  hasAccount: string;
  login: string;
  passwordMismatch: string;
  accountCreated: string;
  verifyEmail: string;
  errorOccurred: string;
  connectionError: string;
}> = {
  tr: {
    pageTitle: "Kayıt Ol – 3D-labX",
    pageDesc: "3D-labX topluluğuna katılın.",
    title: "Hesap Oluştur",
    subtitle: "3D-labX topluluğuna katılın",
    usernameLabel: "Kullanıcı Adı",
    usernamePlaceholder: "ornek_kullanici",
    usernameHint: "3-20 karakter, harf, rakam ve alt çizgi",
    emailLabel: "E-posta",
    emailPlaceholder: "ornek@email.com",
    passwordLabel: "Şifre",
    passwordPlaceholder: "En az 8 karakter",
    passwordConfirmLabel: "Şifre Tekrar",
    passwordConfirmPlaceholder: "Şifrenizi tekrar girin",
    showPassword: "Şifreyi göster",
    privacyPolicy: "Gizlilik Politikası",
    privacyAccept: "'nı okudum ve kabul ediyorum",
    registerButton: "Kayıt Ol",
    hasAccount: "Zaten hesabınız var mı?",
    login: "Giriş Yap",
    passwordMismatch: "Şifreler eşleşmiyor",
    accountCreated: "Hesabınız oluşturuldu!",
    verifyEmail: "Lütfen adresine gönderilen doğrulama linkine tıklayın.",
    errorOccurred: "Bir hata oluştu",
    connectionError: "Bağlantı hatası. Lütfen tekrar deneyin."
  },
  en: {
    pageTitle: "Sign Up – 3D-labX",
    pageDesc: "Join the 3D-labX community.",
    title: "Create Account",
    subtitle: "Join the 3D-labX community",
    usernameLabel: "Username",
    usernamePlaceholder: "example_user",
    usernameHint: "3-20 characters, letters, numbers and underscore",
    emailLabel: "Email",
    emailPlaceholder: "example@email.com",
    passwordLabel: "Password",
    passwordPlaceholder: "At least 8 characters",
    passwordConfirmLabel: "Confirm Password",
    passwordConfirmPlaceholder: "Enter your password again",
    showPassword: "Show password",
    privacyPolicy: "Privacy Policy",
    privacyAccept: " and accept it",
    registerButton: "Sign Up",
    hasAccount: "Already have an account?",
    login: "Login",
    passwordMismatch: "Passwords do not match",
    accountCreated: "Your account has been created!",
    verifyEmail: "Please click the verification link sent to your email.",
    errorOccurred: "An error occurred",
    connectionError: "Connection error. Please try again."
  },
  de: {
    pageTitle: "Registrieren – 3D-labX",
    pageDesc: "Treten Sie der 3D-labX-Community bei.",
    title: "Konto erstellen",
    subtitle: "Treten Sie der 3D-labX-Community bei",
    usernameLabel: "Benutzername",
    usernamePlaceholder: "beispiel_benutzer",
    usernameHint: "3-20 Zeichen, Buchstaben, Zahlen und Unterstrich",
    emailLabel: "E-Mail",
    emailPlaceholder: "beispiel@email.com",
    passwordLabel: "Passwort",
    passwordPlaceholder: "Mindestens 8 Zeichen",
    passwordConfirmLabel: "Passwort bestätigen",
    passwordConfirmPlaceholder: "Geben Sie Ihr Passwort erneut ein",
    showPassword: "Passwort anzeigen",
    privacyPolicy: "Datenschutzrichtlinie",
    privacyAccept: " gelesen und akzeptiert",
    registerButton: "Registrieren",
    hasAccount: "Haben Sie bereits ein Konto?",
    login: "Anmelden",
    passwordMismatch: "Passwörter stimmen nicht überein",
    accountCreated: "Ihr Konto wurde erstellt!",
    verifyEmail: "Bitte klicken Sie auf den Bestätigungslink, der an Ihre E-Mail gesendet wurde.",
    errorOccurred: "Ein Fehler ist aufgetreten",
    connectionError: "Verbindungsfehler. Bitte versuchen Sie es erneut."
  }
};

const t = translations[lang];
---

<BaseLayout title={t.pageTitle} description={t.pageDesc} noIndex={true}>
  <Header />

  <main class="auth-page">
    <div class="auth-container">
      <div class="auth-card">
        <div class="auth-header">
          <h1>{t.title}</h1>
          <p>{t.subtitle}</p>
        </div>

        <form id="registerForm" class="auth-form">
          <div class="form-group">
            <label for="username">{t.usernameLabel}</label>
            <input
              type="text"
              id="username"
              name="username"
              required
              minlength="3"
              maxlength="20"
              pattern="[a-zA-Z0-9_]+"
              placeholder={t.usernamePlaceholder}
              autocomplete="username"
            />
            <span class="form-hint">{t.usernameHint}</span>
          </div>

          <div class="form-group">
            <label for="email">{t.emailLabel}</label>
            <input
              type="email"
              id="email"
              name="email"
              required
              placeholder={t.emailPlaceholder}
              autocomplete="email"
            />
          </div>

          <div class="form-group">
            <label for="password">{t.passwordLabel}</label>
            <div class="password-input">
              <input
                type="password"
                id="password"
                name="password"
                required
                minlength="8"
                placeholder={t.passwordPlaceholder}
                autocomplete="new-password"
              />
              <button type="button" class="toggle-password" aria-label={t.showPassword}>
                <svg class="icon-eye" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"/><circle cx="12" cy="12" r="3"/></svg>
                <svg class="icon-eye-off" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="display:none"><path d="M9.88 9.88a3 3 0 1 0 4.24 4.24"/><path d="M10.73 5.08A10.43 10.43 0 0 1 12 5c7 0 10 7 10 7a13.16 13.16 0 0 1-1.67 2.68"/><path d="M6.61 6.61A13.526 13.526 0 0 0 2 12s3 7 10 7a9.74 9.74 0 0 0 5.39-1.61"/><line x1="2" x2="22" y1="2" y2="22"/></svg>
              </button>
            </div>
          </div>

          <div class="form-group">
            <label for="passwordConfirm">{t.passwordConfirmLabel}</label>
            <input
              type="password"
              id="passwordConfirm"
              name="passwordConfirm"
              required
              minlength="8"
              placeholder={t.passwordConfirmPlaceholder}
              autocomplete="new-password"
            />
          </div>

          <div class="form-group checkbox-group">
            <label class="checkbox-label">
              <input type="checkbox" id="terms" name="terms" required />
              <span><a href="/gizlilik" target="_blank">{t.privacyPolicy}</a>{t.privacyAccept}</span>
            </label>
          </div>

          <div class="form-message" id="formMessage"></div>

          <button type="submit" class="btn btn-primary btn-full" id="submitBtn">
            <span class="btn-text">{t.registerButton}</span>
            <span class="btn-loading" style="display:none">
              <svg class="spinner" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="3" fill="none" stroke-dasharray="32" stroke-linecap="round"/></svg>
            </span>
          </button>
        </form>

        <div class="auth-footer">
          <p>{t.hasAccount} <a href="/auth/giris">{t.login}</a></p>
        </div>
      </div>
    </div>
  </main>
</BaseLayout>

<script define:vars={{ jsT: { passwordMismatch: t.passwordMismatch, accountCreated: t.accountCreated, verifyEmail: t.verifyEmail, errorOccurred: t.errorOccurred, connectionError: t.connectionError } }}>
  const t = jsT;
  const form = document.getElementById('registerForm');
  const message = document.getElementById('formMessage');
  const submitBtn = document.getElementById('submitBtn');
  const btnText = submitBtn.querySelector('.btn-text');
  const btnLoading = submitBtn.querySelector('.btn-loading');

  // Toggle password visibility
  document.querySelectorAll('.toggle-password').forEach(btn => {
    btn.addEventListener('click', () => {
      const input = btn.previousElementSibling;
      const eyeIcon = btn.querySelector('.icon-eye');
      const eyeOffIcon = btn.querySelector('.icon-eye-off');

      if (input.type === 'password') {
        input.type = 'text';
        eyeIcon.style.display = 'none';
        eyeOffIcon.style.display = 'block';
      } else {
        input.type = 'password';
        eyeIcon.style.display = 'block';
        eyeOffIcon.style.display = 'none';
      }
    });
  });

  form.addEventListener('submit', async (e) => {
    e.preventDefault();

    const username = document.getElementById('username').value;
    const email = document.getElementById('email').value;
    const password = document.getElementById('password').value;
    const passwordConfirm = document.getElementById('passwordConfirm').value;

    // Client-side validation
    if (password !== passwordConfirm) {
      message.className = 'form-message error';
      message.textContent = t.passwordMismatch;
      return;
    }

    // Loading state
    btnText.style.display = 'none';
    btnLoading.style.display = 'flex';
    submitBtn.disabled = true;
    message.className = 'form-message';
    message.textContent = '';

    try {
      const res = await fetch('https://tech-portal-api.turgut-d01.workers.dev/api/auth/register', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ username, email, password })
      });

      const data = await res.json();

      if (res.ok) {
        message.className = 'form-message success';
        message.innerHTML = `
          <strong>${t.accountCreated}</strong>
          <p>${t.verifyEmail.replace('adresine', `<strong>${email}</strong> adresine`)}</p>
        `;
        form.reset();
      } else {
        message.className = 'form-message error';
        message.textContent = data.error || t.errorOccurred;
      }
    } catch (err) {
      message.className = 'form-message error';
      message.textContent = t.connectionError;
    } finally {
      btnText.style.display = 'inline';
      btnLoading.style.display = 'none';
      submitBtn.disabled = false;
    }
  });
</script>

<style>
  .auth-page {
    padding-top: 136px;
    padding-bottom: 64px;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(34, 197, 94, 0.05) 0%, rgba(37, 99, 235, 0.05) 100%);
    position: relative;
  }

  .auth-page::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background:
      radial-gradient(circle at 20% 80%, rgba(34, 197, 94, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(37, 99, 235, 0.1) 0%, transparent 50%);
    pointer-events: none;
  }

  .auth-container {
    width: 100%;
    max-width: 440px;
    padding: 0 var(--spacing-lg);
    position: relative;
    z-index: 1;
  }

  .auth-card {
    background: var(--bg-card);
    border: 1px solid var(--border-soft);
    border-radius: 24px;
    padding: var(--spacing-2xl);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
    position: relative;
    overflow: hidden;
  }

  .auth-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #22c55e, var(--accent));
  }

  .auth-header {
    text-align: center;
    margin-bottom: var(--spacing-xl);
  }

  .auth-header h1 {
    font-size: 1.75rem;
    margin-bottom: var(--spacing-xs);
    background: linear-gradient(135deg, var(--text-main) 0%, #22c55e 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .auth-header p {
    color: var(--text-muted);
  }

  .auth-form {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .form-group label {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-main);
  }

  .form-group input[type="text"],
  .form-group input[type="email"],
  .form-group input[type="password"] {
    padding: 12px 14px;
    background: var(--bg-main);
    border: 1px solid var(--border-soft);
    border-radius: var(--radius-md);
    color: var(--text-main);
    font-size: 1rem;
    transition: border-color var(--transition-fast);
  }

  .form-group input:focus {
    outline: none;
    border-color: var(--accent);
  }

  .form-hint {
    font-size: 0.75rem;
    color: var(--text-light);
  }

  .password-input {
    position: relative;
    display: flex;
    align-items: center;
  }

  .password-input input {
    width: 100%;
    padding-right: 44px;
  }

  .toggle-password {
    position: absolute;
    right: 12px;
    background: none;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 4px;
  }

  .toggle-password:hover {
    color: var(--text-main);
  }

  .checkbox-group {
    flex-direction: row;
    align-items: flex-start;
  }

  .checkbox-label {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-sm);
    font-size: 0.85rem;
    color: var(--text-muted);
    cursor: pointer;
  }

  .checkbox-label input {
    margin-top: 3px;
  }

  .checkbox-label a {
    color: var(--accent);
    text-decoration: none;
  }

  .checkbox-label a:hover {
    text-decoration: underline;
  }

  .form-message {
    padding: var(--spacing-md);
    border-radius: var(--radius-md);
    font-size: 0.9rem;
    display: none;
  }

  .form-message:not(:empty) {
    display: block;
  }

  .form-message.error {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.3);
    color: #ef4444;
  }

  .form-message.success {
    background: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.3);
    color: #22c55e;
  }

  .form-message p {
    margin: var(--spacing-xs) 0 0;
    font-size: 0.85rem;
  }

  .btn-full {
    width: 100%;
    justify-content: center;
    margin-top: var(--spacing-sm);
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
    border: none;
    padding: 14px 20px;
    border-radius: 12px;
    font-weight: 600;
    box-shadow: 0 4px 15px rgba(34, 197, 94, 0.3);
    transition: all 0.2s;
  }

  .btn-full:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(34, 197, 94, 0.4);
  }

  .btn-loading {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .spinner {
    width: 20px;
    height: 20px;
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }

  .auth-footer {
    text-align: center;
    margin-top: var(--spacing-xl);
    padding-top: var(--spacing-lg);
    border-top: 1px solid var(--border-soft);
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .auth-footer a {
    color: var(--accent);
    text-decoration: none;
    font-weight: 500;
  }

  .auth-footer a:hover {
    text-decoration: underline;
  }

  @media (max-width: 480px) {
    .auth-card {
      padding: var(--spacing-lg);
    }
  }
</style>
