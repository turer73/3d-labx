---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Header from "../../components/Header.astro";
import { getApiBase } from "../../lib/api";

type Post = {
  id: number;
  title_tr: string;
  summary_tr: string;
  slug: string;
  category: string;
  image_url?: string;
  is_featured: number;
  created_at?: string;
};

type Pagination = {
  page: number;
  limit: number;
  total: number;
  totalPages: number;
  hasNext: boolean;
  hasPrev: boolean;
};

type ApiResponse = {
  posts: Post[];
  pagination: Pagination;
};

const API_BASE = getApiBase(Astro.request);

const url = new URL(Astro.request.url);
const currentPage = Math.max(1, parseInt(url.searchParams.get("page") || "1", 10));
const POSTS_PER_PAGE = 12;

let posts: Post[] = [];
let pagination: Pagination | null = null;
let error: string | null = null;

try {
  const res = await fetch(
    `${API_BASE}/api/posts?category=sorun-cozumleri&page=${currentPage}&limit=${POSTS_PER_PAGE}`,
    { headers: { "Accept": "application/json" } }
  );

  if (!res.ok) {
    throw new Error(`API hatası: ${res.status}`);
  }

  const data: ApiResponse = await res.json();
  posts = data.posts;
  pagination = data.pagination;
} catch (e) {
  error = e instanceof Error ? e.message : "Bilinmeyen hata";
  console.error("Sorun Çözümleri sayfası hatası:", error);
}

// SEO için optimize edilmiş başlık ve açıklama
const pageTitle = currentPage > 1
  ? `3D Yazıcı Sorun Çözümleri - Sayfa ${currentPage} | 3D-labX`
  : "3D Yazıcı Sorun Çözümleri ve Troubleshooting Rehberleri | 3D-labX";

const pageDescription = "3D yazıcı sorunlarına kapsamlı çözümler: Nozzle tıkanıklığı, ilk katman yapışmama, ipliklenme, katman kayması ve daha fazlası. Bambu Lab, Creality, Elegoo, Anycubic için marka bazlı rehberler.";

const prevUrl = pagination?.hasPrev ? `/sorun-cozumleri?page=${currentPage - 1}` : null;
const nextUrl = pagination?.hasNext ? `/sorun-cozumleri?page=${currentPage + 1}` : null;

// Schema.org yapılandırılmış veri
const schemaData = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "3D Yazıcı Sorun Çözümleri",
  "description": pageDescription,
  "url": "https://3d-labx.com/sorun-cozumleri",
  "publisher": {
    "@type": "Organization",
    "name": "3D-labX",
    "url": "https://3d-labx.com"
  },
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": [
      {
        "@type": "ListItem",
        "position": 1,
        "name": "Bambu Lab Sorun Giderme Rehberi",
        "url": "https://3d-labx.com/sorun-cozumleri/bambu-lab-sorun-giderme"
      },
      {
        "@type": "ListItem",
        "position": 2,
        "name": "Creality Sorun Giderme Rehberi",
        "url": "https://3d-labx.com/sorun-cozumleri/creality-sorun-giderme"
      },
      {
        "@type": "ListItem",
        "position": 3,
        "name": "Elegoo Sorun Giderme Rehberi",
        "url": "https://3d-labx.com/sorun-cozumleri/elegoo-sorun-giderme"
      },
      {
        "@type": "ListItem",
        "position": 4,
        "name": "Anycubic Sorun Giderme Rehberi",
        "url": "https://3d-labx.com/sorun-cozumleri/anycubic-sorun-giderme"
      }
    ]
  }
};

// Marka verileri
const brands = [
  {
    name: "Bambu Lab",
    slug: "bambu-lab-sorun-giderme",
    logo: "/brand-logos/bambu-lab-logo.webp",
    color: "#00AE42",
    description: "X1, P1, A1 serisi için kapsamlı sorun giderme",
    problems: ["Tabla Yapışma", "Nozzle Tıkanıklığı", "AMS Sorunları"]
  },
  {
    name: "Creality",
    slug: "creality-sorun-giderme",
    logo: "/brand-logos/creality-logo.png",
    color: "#22c55e",
    description: "Ender 3, CR-10 serisi için çözümler",
    problems: ["İlk Katman", "Ekstrüzyon", "Seviyeleme"]
  },
  {
    name: "Elegoo",
    slug: "elegoo-sorun-giderme",
    logo: "/brand-logos/elegoo-logo.jpg",
    color: "#3B82F6",
    description: "Neptune FDM ve Mars/Saturn Reçine rehberi",
    problems: ["LCD Arızası", "FEP Film", "Yapışma"]
  },
  {
    name: "Anycubic",
    slug: "anycubic-sorun-giderme",
    logo: "/brand-logos/anycubic-logo.webp",
    color: "#f59e0b",
    description: "Kobra, Vyper, Mega serisi için rehber",
    problems: ["Seviyeleme", "Ekstrüzyon", "Katman Kayması"]
  }
];
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <Header />

  <!-- Schema.org JSON-LD -->
  <script type="application/ld+json" set:html={JSON.stringify(schemaData)} />

  <main class="troubleshooting-page" itemscope itemtype="https://schema.org/CollectionPage">
    <div class="container">

      <!-- Hero Section -->
      <header class="page-hero">
        <div class="hero-content">
          <nav class="breadcrumb" aria-label="Breadcrumb">
            <ol itemscope itemtype="https://schema.org/BreadcrumbList">
              <li itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                <a href="/" itemprop="item"><span itemprop="name">Ana Sayfa</span></a>
                <meta itemprop="position" content="1" />
              </li>
              <li itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                <span itemprop="name" aria-current="page">Sorun Çözümleri</span>
                <meta itemprop="position" content="2" />
              </li>
            </ol>
          </nav>
          <h1 itemprop="name">3D Yazıcı Sorun Çözümleri</h1>
          <p class="hero-desc" itemprop="description">
            3D baskı sorunlarınıza kapsamlı çözümler. Marka bazlı rehberler ve adım adım troubleshooting kılavuzları.
          </p>
          <div class="hero-stats">
            <div class="stat">
              <span class="stat-number">4</span>
              <span class="stat-label">Marka Rehberi</span>
            </div>
            <div class="stat">
              <span class="stat-number">{(pagination?.total || 0) + 24}</span>
              <span class="stat-label">Çözüm</span>
            </div>
            <div class="stat">
              <span class="stat-number">6</span>
              <span class="stat-label">Kategori</span>
            </div>
          </div>
        </div>
        <div class="hero-visual">
          <div class="hero-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
            </svg>
          </div>
        </div>
      </header>

      <!-- Bento Grid: Marka Rehberleri -->
      <section class="bento-section" aria-labelledby="brand-guides-title">
        <h2 id="brand-guides-title" class="section-title">
          <span class="title-icon">🏭</span>
          Marka Bazlı Rehberler
        </h2>

        <div class="bento-grid">
          {brands.map((brand, index) => (
            <article
              class={`bento-card brand-card ${index === 0 ? 'featured' : ''}`}
              style={`--brand-color: ${brand.color}`}
              itemscope
              itemtype="https://schema.org/HowTo"
            >
              <a href={`/sorun-cozumleri/${brand.slug}`} class="card-link">
                <div class="card-header">
                  <div class="brand-logo">
                    <img src={brand.logo} alt={`${brand.name} Logo`} loading="lazy" width="120" height="60" />
                  </div>
                  <span class="card-badge" style={`background: ${brand.color}`}>Rehber</span>
                </div>
                <div class="card-body">
                  <h3 itemprop="name">{brand.name} Sorun Giderme</h3>
                  <p itemprop="description">{brand.description}</p>
                  <div class="problem-tags">
                    {brand.problems.map(problem => (
                      <span class="tag">{problem}</span>
                    ))}
                  </div>
                </div>
                <div class="card-footer">
                  <span class="view-link">
                    Rehberi İncele
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="m9 18 6-6-6-6"/>
                    </svg>
                  </span>
                </div>
              </a>
            </article>
          ))}
        </div>
      </section>

      <!-- Popüler Sorunlar Quick Links -->
      <section class="quick-links-section" aria-labelledby="popular-problems-title">
        <h2 id="popular-problems-title" class="section-title">
          <span class="title-icon">🔥</span>
          Sık Karşılaşılan Sorunlar
        </h2>

        <div class="quick-links-grid">
          <a href="/sorun-cozumleri/3d-yazici-nozzle-tikanikligi-kabustan-kurtulmanin-kesin-yollari-2026-rehberi-mkyiw0om" class="quick-link">
            <span class="quick-icon">🔧</span>
            <span class="quick-text">Nozzle Tıkanıklığı</span>
          </a>
          <a href="/sorun-cozumleri/3d-baskida-i-lk-katman-yapisma-sorununa-kokten-cozum-rehberi-mkxazxot" class="quick-link">
            <span class="quick-icon">📐</span>
            <span class="quick-text">İlk Katman Yapışmıyor</span>
          </a>
          <a href="/sorun-cozumleri/3d-baskida-i-pliklenme-stringing-sorununa-kesin-cozum-rehberi-mkx4vmgl" class="quick-link">
            <span class="quick-icon">🕸️</span>
            <span class="quick-text">İpliklenme (Stringing)</span>
          </a>
          <a href="/sorun-cozumleri/3d-yazicida-abs-filament-ile-baskida-catlama-ve-warping-sorunlarina-cozum-mkxazkpg" class="quick-link">
            <span class="quick-icon">🌡️</span>
            <span class="quick-text">Warping / Çarpılma</span>
          </a>
        </div>
      </section>

      <!-- Tüm Çözüm Rehberleri -->
      {posts.length > 0 && (
        <section class="posts-section" aria-labelledby="all-guides-title">
          <h2 id="all-guides-title" class="section-title">
            <span class="title-icon">📚</span>
            Tüm Çözüm Rehberleri
          </h2>

          <div class="posts-bento-grid">
            {posts.map((post, index) => (
              <article
                class={`post-card ${index === 0 ? 'large' : ''}`}
                itemscope
                itemtype="https://schema.org/Article"
              >
                {post.image_url && (
                  <a href={`/sorun-cozumleri/${post.slug}`} class="post-image">
                    <img
                      src={post.image_url}
                      alt={post.title_tr}
                      loading="lazy"
                      itemprop="image"
                    />
                  </a>
                )}
                <div class="post-content">
                  <h3 itemprop="headline">
                    <a href={`/sorun-cozumleri/${post.slug}`} itemprop="url">{post.title_tr}</a>
                  </h3>
                  <p itemprop="description">{post.summary_tr}</p>
                  {post.created_at && (
                    <time class="post-date" datetime={post.created_at} itemprop="datePublished">
                      {new Date(post.created_at).toLocaleDateString("tr-TR", {
                        year: "numeric",
                        month: "long",
                        day: "numeric"
                      })}
                    </time>
                  )}
                </div>
              </article>
            ))}
          </div>
        </section>
      )}

      {error && (
        <div class="message message-error" role="alert">
          <p><strong>İçerikler yüklenirken bir hata oluştu.</strong></p>
          <p class="text-sm">{error}</p>
        </div>
      )}

      <!-- Pagination -->
      {pagination && pagination.totalPages > 1 && (
        <nav class="pagination" aria-label="Sayfa navigasyonu">
          <div class="pagination-wrapper">
            <a
              href={prevUrl || "#"}
              class={`pagination-btn ${!prevUrl ? 'disabled' : ''}`}
              aria-disabled={!prevUrl}
              rel={prevUrl ? "prev" : undefined}
            >
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                <path d="m15 18-6-6 6-6"/>
              </svg>
              <span>Önceki</span>
            </a>

            <div class="pagination-info">
              <span class="current-page">{pagination.page}</span>
              <span class="separator">/</span>
              <span class="total-pages">{pagination.totalPages}</span>
            </div>

            <a
              href={nextUrl || "#"}
              class={`pagination-btn ${!nextUrl ? 'disabled' : ''}`}
              aria-disabled={!nextUrl}
              rel={nextUrl ? "next" : undefined}
            >
              <span>Sonraki</span>
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                <path d="m9 18 6-6-6-6"/>
              </svg>
            </a>
          </div>
        </nav>
      )}

      <!-- CTA Section -->
      <section class="cta-section">
        <div class="cta-content">
          <h2>Sorununuzu Bulamadınız mı?</h2>
          <p>Topluluğumuza katılın, deneyimli maker'lardan yardım alın.</p>
          <div class="cta-buttons">
            <a href="/topluluk/forum" class="btn btn-primary">Foruma Git</a>
            <a href="/iletisim" class="btn btn-outline">İletişime Geç</a>
          </div>
        </div>
      </section>

    </div>
  </main>
</BaseLayout>

<style>
  .troubleshooting-page {
    padding-top: 120px;
    padding-bottom: 64px;
    min-height: 100vh;
  }

  /* Hero Section */
  .page-hero {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 48px;
    align-items: center;
    padding: 48px;
    background: linear-gradient(135deg, rgba(168, 85, 247, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
    border-radius: 24px;
    margin-bottom: 48px;
    border: 1px solid var(--border-soft);
  }

  .breadcrumb ol {
    display: flex;
    gap: 8px;
    list-style: none;
    padding: 0;
    margin: 0 0 16px 0;
    font-size: 0.875rem;
  }

  .breadcrumb li {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--text-muted);
  }

  .breadcrumb li::after {
    content: '/';
    color: var(--text-light);
  }

  .breadcrumb li:last-child::after {
    display: none;
  }

  .breadcrumb a {
    color: var(--text-muted);
    text-decoration: none;
  }

  .breadcrumb a:hover {
    color: var(--accent);
  }

  .page-hero h1 {
    font-size: 2.5rem;
    font-weight: 800;
    margin: 0 0 16px 0;
    background: linear-gradient(135deg, var(--text-main) 0%, var(--accent) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-desc {
    font-size: 1.125rem;
    color: var(--text-muted);
    margin: 0 0 24px 0;
    max-width: 500px;
    line-height: 1.6;
  }

  .hero-stats {
    display: flex;
    gap: 32px;
  }

  .stat {
    display: flex;
    flex-direction: column;
  }

  .stat-number {
    font-size: 2rem;
    font-weight: 800;
    color: var(--accent);
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--text-muted);
  }

  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-icon {
    width: 120px;
    height: 120px;
    background: linear-gradient(135deg, var(--accent) 0%, #8b5cf6 100%);
    border-radius: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
  }

  .hero-icon svg {
    width: 64px;
    height: 64px;
  }

  /* Section Titles */
  .section-title {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-main);
    margin-bottom: 24px;
  }

  .title-icon {
    font-size: 1.25rem;
  }

  /* Bento Grid */
  .bento-section {
    margin-bottom: 48px;
  }

  .bento-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-template-rows: auto;
    gap: 20px;
  }

  .bento-card {
    background: var(--bg-card);
    border: 2px solid var(--border-soft);
    border-radius: 20px;
    overflow: hidden;
    transition: all 0.3s ease;
    position: relative;
  }

  .bento-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--brand-color);
    opacity: 0;
    transition: opacity 0.3s;
  }

  .bento-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.15);
    border-color: var(--brand-color);
  }

  .bento-card:hover::before {
    opacity: 1;
  }

  .bento-card.featured {
    grid-column: span 2;
  }

  .card-link {
    display: flex;
    flex-direction: column;
    height: 100%;
    text-decoration: none;
    color: inherit;
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 20px 20px 0;
  }

  .brand-logo {
    height: 50px;
    display: flex;
    align-items: center;
  }

  .brand-logo img {
    max-height: 100%;
    width: auto;
    object-fit: contain;
  }

  .card-badge {
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    color: white;
  }

  .card-body {
    flex: 1;
    padding: 16px 20px;
  }

  .card-body h3 {
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--text-main);
    margin: 0 0 8px 0;
  }

  .card-body p {
    font-size: 0.875rem;
    color: var(--text-muted);
    margin: 0 0 12px 0;
    line-height: 1.5;
  }

  .problem-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .tag {
    padding: 4px 10px;
    background: var(--bg-secondary);
    border-radius: 6px;
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  .card-footer {
    padding: 16px 20px;
    border-top: 1px solid var(--border-soft);
  }

  .view-link {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--brand-color);
  }

  .view-link svg {
    width: 16px;
    height: 16px;
    transition: transform 0.3s;
  }

  .bento-card:hover .view-link svg {
    transform: translateX(4px);
  }

  /* Quick Links */
  .quick-links-section {
    margin-bottom: 48px;
  }

  .quick-links-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
  }

  .quick-link {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 16px 20px;
    background: var(--bg-card);
    border: 1px solid var(--border-soft);
    border-radius: 12px;
    text-decoration: none;
    color: var(--text-main);
    transition: all 0.2s;
  }

  .quick-link:hover {
    border-color: var(--accent);
    background: rgba(168, 85, 247, 0.05);
    transform: translateY(-2px);
  }

  .quick-icon {
    font-size: 1.25rem;
  }

  .quick-text {
    font-size: 0.9rem;
    font-weight: 500;
  }

  /* Posts Bento Grid */
  .posts-section {
    margin-bottom: 48px;
  }

  .posts-bento-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  .post-card {
    background: var(--bg-card);
    border: 1px solid var(--border-soft);
    border-radius: 16px;
    overflow: hidden;
    transition: all 0.3s;
  }

  .post-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0,0,0,0.1);
  }

  .post-card.large {
    grid-column: span 2;
  }

  .post-card.large .post-image {
    aspect-ratio: 21/9;
  }

  .post-image {
    display: block;
    aspect-ratio: 16/9;
    overflow: hidden;
  }

  .post-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s;
  }

  .post-card:hover .post-image img {
    transform: scale(1.05);
  }

  .post-content {
    padding: 20px;
  }

  .post-content h3 {
    font-size: 1rem;
    font-weight: 600;
    margin: 0 0 8px 0;
    line-height: 1.4;
  }

  .post-content h3 a {
    color: var(--text-main);
    text-decoration: none;
  }

  .post-content h3 a:hover {
    color: var(--accent);
  }

  .post-content p {
    font-size: 0.875rem;
    color: var(--text-muted);
    margin: 0;
    line-height: 1.6;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .post-date {
    display: block;
    margin-top: 12px;
    font-size: 0.8rem;
    color: var(--text-light);
  }

  /* Pagination */
  .pagination {
    display: flex;
    justify-content: center;
    margin-bottom: 48px;
  }

  .pagination-wrapper {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 8px;
    background: var(--bg-card);
    border: 1px solid var(--border-soft);
    border-radius: 12px;
  }

  .pagination-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 10px 16px;
    background: transparent;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-main);
    text-decoration: none;
    transition: all 0.2s;
  }

  .pagination-btn:hover:not(.disabled) {
    background: var(--bg-secondary);
  }

  .pagination-btn.disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  .pagination-btn svg {
    width: 18px;
    height: 18px;
  }

  .pagination-info {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.9rem;
  }

  .current-page {
    font-weight: 700;
    color: var(--accent);
  }

  .separator {
    color: var(--text-light);
  }

  /* CTA Section */
  .cta-section {
    background: linear-gradient(135deg, var(--accent) 0%, #8b5cf6 100%);
    border-radius: 20px;
    padding: 48px;
    text-align: center;
    color: white;
  }

  .cta-content h2 {
    font-size: 1.75rem;
    font-weight: 700;
    margin: 0 0 12px 0;
  }

  .cta-content p {
    font-size: 1.1rem;
    opacity: 0.9;
    margin: 0 0 24px 0;
  }

  .cta-buttons {
    display: flex;
    justify-content: center;
    gap: 16px;
  }

  .cta-buttons .btn {
    padding: 12px 28px;
    border-radius: 10px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s;
  }

  .cta-buttons .btn-primary {
    background: white;
    color: var(--accent);
  }

  .cta-buttons .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(0,0,0,0.2);
  }

  .cta-buttons .btn-outline {
    background: transparent;
    border: 2px solid white;
    color: white;
  }

  .cta-buttons .btn-outline:hover {
    background: white;
    color: var(--accent);
  }

  /* Error Message */
  .message-error {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.3);
    border-radius: 12px;
    padding: 20px;
    text-align: center;
    margin-bottom: 48px;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .bento-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .bento-card.featured {
      grid-column: span 2;
    }

    .posts-bento-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .post-card.large {
      grid-column: span 2;
    }

    .quick-links-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .troubleshooting-page {
      padding-top: 100px;
    }

    .page-hero {
      grid-template-columns: 1fr;
      padding: 32px 24px;
      text-align: center;
    }

    .page-hero h1 {
      font-size: 1.75rem;
    }

    .hero-desc {
      max-width: none;
    }

    .hero-stats {
      justify-content: center;
    }

    .hero-visual {
      display: none;
    }

    .bento-grid {
      grid-template-columns: 1fr;
    }

    .bento-card.featured {
      grid-column: span 1;
    }

    .quick-links-grid {
      grid-template-columns: 1fr;
    }

    .posts-bento-grid {
      grid-template-columns: 1fr;
    }

    .post-card.large {
      grid-column: span 1;
    }

    .post-card.large .post-image {
      aspect-ratio: 16/9;
    }

    .cta-section {
      padding: 32px 24px;
    }

    .cta-buttons {
      flex-direction: column;
    }
  }
</style>
