---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Header from "../../components/Header.astro";
import { getApiBase } from "../../lib/api";

type Post = {
  id: number;
  title_tr: string;
  summary_tr: string;
  slug: string;
  category: string;
  image_url?: string;
  is_featured: number;
  created_at?: string;
};

type Pagination = {
  page: number;
  limit: number;
  total: number;
  totalPages: number;
  hasNext: boolean;
  hasPrev: boolean;
};

type ApiResponse = {
  posts: Post[];
  pagination: Pagination;
};

const API_BASE = getApiBase(Astro.request);

const url = new URL(Astro.request.url);
const currentPage = Math.max(1, parseInt(url.searchParams.get("page") || "1", 10));
const POSTS_PER_PAGE = 12;

let posts: Post[] = [];
let pagination: Pagination | null = null;
let error: string | null = null;

try {
  const res = await fetch(
    `${API_BASE}/api/posts?category=sorun-cozumleri&page=${currentPage}&limit=${POSTS_PER_PAGE}`,
    { headers: { "Accept": "application/json" } }
  );

  if (!res.ok) {
    throw new Error(`API hatası: ${res.status}`);
  }

  const data: ApiResponse = await res.json();
  posts = data.posts;
  pagination = data.pagination;
} catch (e) {
  error = e instanceof Error ? e.message : "Bilinmeyen hata";
  console.error("Sorun Çözümleri sayfası hatası:", error);
}

const title = currentPage > 1
  ? `Sorun Çözümleri - Sayfa ${currentPage} - 3D-labX`
  : "Sorun Çözümleri - 3D-labX";

const description = posts.length > 0
  ? `3D baskı sorunları ve çözümleri: ${posts.slice(0, 3).map((p) => p.title_tr).join(", ")}`
  : "3D baskı sorunları ve çözümleri. Yaygın problemler için adım adım çözüm rehberleri.";

const prevUrl = pagination?.hasPrev ? `/sorun-cozumleri?page=${currentPage - 1}` : null;
const nextUrl = pagination?.hasNext ? `/sorun-cozumleri?page=${currentPage + 1}` : null;
---

<BaseLayout title={title} description={description}>
  <Header />

  <main class="category-page">
    <div class="container">
      <div class="page-header">
        <div class="page-title-group">
          <h1>Sorun Çözümleri</h1>
          <p class="page-desc">3D baskı sorunları ve çözüm rehberleri</p>
        </div>
        {pagination && (
          <span class="post-count">{pagination.total} çözüm</span>
        )}
      </div>

      <!-- Marka Bazlı Rehberler -->
      <section class="featured-guides">
        <h2 class="section-title">Marka Bazlı Rehberler</h2>
        <div class="brand-posts-grid">
          <article class="post-card brand-card bambu">
            <a href="/sorun-cozumleri/bambu-lab-sorun-giderme" class="post-image brand-image">
              <img src="/brand-logos/bambu-lab-logo.svg" alt="Bambu Lab Logo" loading="lazy" />
              <span class="brand-overlay bambu-overlay"></span>
            </a>
            <div class="post-content">
              <span class="badge badge-brand bambu-badge">Çözüm Rehberi</span>
              <h2>
                <a href="/sorun-cozumleri/bambu-lab-sorun-giderme">Bambu Lab Yazıcılarda Sık Karşılaşılan Sorunlar ve Çözümleri</a>
              </h2>
              <p>X1, P1, A1 serisi yazıcılar için kapsamlı sorun giderme rehberi. Tabla yapışma, nozzle tıkanıklığı, AMS sorunları ve daha fazlası.</p>
              <time class="post-date">Güncel Rehber</time>
            </div>
          </article>

          <article class="post-card brand-card creality">
            <a href="/sorun-cozumleri/creality-sorun-giderme" class="post-image brand-image">
              <img src="/brand-logos/creality-logo.svg" alt="Creality Logo" loading="lazy" />
              <span class="brand-overlay creality-overlay"></span>
            </a>
            <div class="post-content">
              <span class="badge badge-brand creality-badge">Çözüm Rehberi</span>
              <h2>
                <a href="/sorun-cozumleri/creality-sorun-giderme">Creality Yazıcılarda Sık Karşılaşılan Sorunlar ve Çözümleri</a>
              </h2>
              <p>Ender 3, CR-10 serisi yazıcılar için kapsamlı sorun giderme rehberi. İlk katman, ekstrüzyon ve seviyeleme sorunları.</p>
              <time class="post-date">Güncel Rehber</time>
            </div>
          </article>

          <article class="post-card brand-card elegoo">
            <a href="/sorun-cozumleri/elegoo-sorun-giderme" class="post-image brand-image">
              <img src="/brand-logos/elegoo-logo.svg" alt="Elegoo Logo" loading="lazy" />
              <span class="brand-overlay elegoo-overlay"></span>
            </a>
            <div class="post-content">
              <span class="badge badge-brand elegoo-badge">Çözüm Rehberi</span>
              <h2>
                <a href="/sorun-cozumleri/elegoo-sorun-giderme">Elegoo Yazıcılarda Sık Karşılaşılan Sorunlar ve Çözümleri</a>
              </h2>
              <p>Neptune (FDM) ve Mars/Saturn (Reçine) serisi için kapsamlı rehber. LCD, FEP ve baskı sorunları çözümleri.</p>
              <time class="post-date">Güncel Rehber</time>
            </div>
          </article>

          <article class="post-card brand-card anycubic">
            <a href="/sorun-cozumleri/anycubic-sorun-giderme" class="post-image brand-image">
              <img src="/brand-logos/anycubic-logo.svg" alt="Anycubic Logo" loading="lazy" />
              <span class="brand-overlay anycubic-overlay"></span>
            </a>
            <div class="post-content">
              <span class="badge badge-brand anycubic-badge">Çözüm Rehberi</span>
              <h2>
                <a href="/sorun-cozumleri/anycubic-sorun-giderme">Anycubic Yazıcılarda Sık Karşılaşılan Sorunlar ve Çözümleri</a>
              </h2>
              <p>Kobra, Vyper ve Mega serisi yazıcılar için kapsamlı rehber. Seviyeleme, ekstrüzyon ve katman sorunları.</p>
              <time class="post-date">Güncel Rehber</time>
            </div>
          </article>
        </div>
      </section>

      {error && (
        <div class="message message-error">
          <p><strong>İçerikler yüklenirken bir hata oluştu.</strong></p>
          <p class="text-sm">{error}</p>
        </div>
      )}

      {!error && posts.length === 0 && (
        <div class="empty-state">
          <div class="empty-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
          </div>
          <h2>Henüz Sorun Çözümü Yok</h2>
          <p>Bu kategoride henüz içerik bulunmuyor. Yakında yaygın sorunlar ve çözümleri eklenecek!</p>
          <a href="/" class="btn btn-primary">Ana Sayfaya Dön</a>
        </div>
      )}

      {posts.length > 0 && (
        <div class="posts-grid">
          {posts.map((post) => (
            <article class={`post-card ${post.is_featured ? 'featured' : ''}`}>
              {post.image_url && (
                <a href={`/sorun-cozumleri/${post.slug}`} class="post-image">
                  <img src={post.image_url} alt={post.title_tr} loading="lazy" />
                </a>
              )}
              <div class="post-content">
                {post.is_featured === 1 && (
                  <span class="badge badge-fix">Çözüm Rehberi</span>
                )}
                <h2>
                  <a href={`/sorun-cozumleri/${post.slug}`}>{post.title_tr}</a>
                </h2>
                <p>{post.summary_tr}</p>
                {post.created_at && (
                  <time class="post-date" datetime={post.created_at}>
                    {new Date(post.created_at).toLocaleDateString("tr-TR", {
                      year: "numeric",
                      month: "long",
                      day: "numeric"
                    })}
                  </time>
                )}
              </div>
            </article>
          ))}
        </div>
      )}

      {pagination && pagination.totalPages > 1 && (
        <nav class="pagination" aria-label="Sayfa navigasyonu">
          <div class="pagination-wrapper">
            <a
              href={prevUrl || "#"}
              class={`pagination-btn ${!prevUrl ? 'disabled' : ''}`}
              aria-disabled={!prevUrl}
            >
              <svg class="arrow-left" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
              <span class="btn-text">Önceki</span>
            </a>

            <div class="pagination-info">
              <span class="current-page">{pagination.page}</span>
              <span class="separator">/</span>
              <span class="total-pages">{pagination.totalPages}</span>
            </div>

            <a
              href={nextUrl || "#"}
              class={`pagination-btn ${!nextUrl ? 'disabled' : ''}`}
              aria-disabled={!nextUrl}
            >
              <span class="btn-text">Sonraki</span>
              <svg class="arrow-right" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
            </a>
          </div>
        </nav>
      )}
    </div>
  </main>
</BaseLayout>

<style>
  .category-page {
    padding-top: 156px;
    padding-bottom: 64px;
    min-height: 100vh;
  }

  .page-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: var(--spacing-xl);
    flex-wrap: wrap;
    gap: var(--spacing-sm);
  }

  .page-title-group {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .page-header h1 {
    margin: 0;
  }

  .page-desc {
    color: var(--text-muted);
    font-size: 0.95rem;
    margin: 0;
  }

  .post-count {
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--spacing-lg);
  }

  .post-card {
    background: var(--bg-card);
    border: 1px solid var(--border-soft);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: transform var(--transition-base), box-shadow var(--transition-base);
  }

  .post-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .post-card.featured {
    border-color: #a855f7;
    border-width: 2px;
  }

  .post-image {
    display: block;
    aspect-ratio: 16/9;
    overflow: hidden;
  }

  .post-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .post-card:hover .post-image img {
    transform: scale(1.05);
  }

  .post-content {
    padding: var(--spacing-lg);
  }

  .badge-fix {
    background: #a855f7;
    color: #fff;
    padding: 2px 10px;
    border-radius: 4px;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .post-content h2 {
    font-size: 1.1rem;
    margin: var(--spacing-sm) 0;
    line-height: 1.4;
  }

  .post-content h2 a {
    color: var(--text-main);
    text-decoration: none;
  }

  .post-content h2 a:hover {
    color: var(--accent);
  }

  .post-content p {
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.6;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .post-date {
    display: block;
    margin-top: var(--spacing-md);
    font-size: 0.8rem;
    color: var(--text-light);
  }

  .empty-state {
    text-align: center;
    padding: var(--spacing-2xl) var(--spacing-lg);
  }

  .empty-icon {
    color: var(--text-light);
    margin-bottom: var(--spacing-md);
    opacity: 0.5;
  }

  .empty-state h2 {
    margin-bottom: var(--spacing-sm);
  }

  .empty-state p {
    color: var(--text-muted);
    margin-bottom: var(--spacing-lg);
  }

  /* Featured Guides Section */
  .featured-guides {
    margin-bottom: var(--spacing-2xl);
  }

  .section-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text-main);
    margin-bottom: var(--spacing-lg);
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .section-title::before {
    content: '';
    width: 4px;
    height: 24px;
    background: linear-gradient(135deg, var(--accent), #8b5cf6);
    border-radius: 2px;
  }

  .brand-posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--spacing-lg);
  }

  .brand-card {
    border-width: 2px;
  }

  .brand-card.bambu {
    border-color: rgba(0, 174, 66, 0.4);
  }

  .brand-card.bambu:hover {
    border-color: #00AE42;
  }

  .brand-card.creality {
    border-color: rgba(34, 197, 94, 0.4);
  }

  .brand-card.creality:hover {
    border-color: #22c55e;
  }

  .brand-card.elegoo {
    border-color: rgba(59, 130, 246, 0.4);
  }

  .brand-card.elegoo:hover {
    border-color: #3B82F6;
  }

  .brand-card.anycubic {
    border-color: rgba(245, 158, 11, 0.4);
  }

  .brand-card.anycubic:hover {
    border-color: #f59e0b;
  }

  .brand-image {
    position: relative;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
  }

  .brand-image img {
    object-fit: contain !important;
    padding: 24px;
    filter: brightness(1.1);
  }

  .brand-overlay {
    position: absolute;
    inset: 0;
    opacity: 0.15;
    pointer-events: none;
  }

  .bambu-overlay {
    background: linear-gradient(135deg, #00AE42 0%, #008833 100%);
  }

  .creality-overlay {
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
  }

  .elegoo-overlay {
    background: linear-gradient(135deg, #3B82F6 0%, #2563eb 100%);
  }

  .anycubic-overlay {
    background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
  }

  .badge-brand {
    padding: 3px 12px;
    border-radius: 4px;
    font-size: 0.75rem;
    font-weight: 600;
    color: #fff;
  }

  .bambu-badge {
    background: #00AE42;
  }

  .creality-badge {
    background: #22c55e;
  }

  .elegoo-badge {
    background: #3B82F6;
  }

  .anycubic-badge {
    background: #f59e0b;
  }

  @media (max-width: 640px) {
    .posts-grid {
      grid-template-columns: 1fr;
    }

    .brand-posts-grid {
      grid-template-columns: 1fr;
    }

    .page-header {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
